#include"minishell.h"

int lexer(char *buffer)
{
    int     i;
    t_token *token;

    i = 0;
    while(buffer[i])
    {
        if (buffer[i] == )
        i++;
    }
    // token = malloc(sizeof(t_token))
    return (0);
}